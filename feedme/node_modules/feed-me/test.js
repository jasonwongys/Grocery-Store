const metro = require('./')

metro.getAllResults({ category: 'beverages/beer-cider' })
  .then(x => JSON.stringify(x, null, 2))
  .then(console.log)
  .catch(e => console.error(e.stack || e))

// metro.getResults({ category: 'beverages/beer-cider', filter: 'IPA' })
//   .then(beers => {
//     console.log(beers)
//   })
// const { busbudBeers, beerMe, beerMeVariety } = require('./')

// // metro.getBeers()
// //   .then(beers => console.log(JSON.stringify(beers, null, 2)))

// // console.log(busbudBeers.map(x => x.weight))

// // const variety = beerMe(busbudBeers.filter(beer => beer.name.includes('variety')), 24)[0]

// // ;[variety, ...beerMe(busbudBeers, 48 - variety.count)]
// beerMeVariety(busbudBeers, 48, 12)
//   .forEach(beer => console.log(beer.brand + ' ' + beer.name + ' ' + beer.weight + ' ' + beer.price + ' ' + beer.link))
